import React from 'react';
import { Box, Typography } from '@mui/material';

const InternetPage: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h3">인터넷 가입 페이지입니다</Typography>
    </Box>
  );
};

export default InternetPage;